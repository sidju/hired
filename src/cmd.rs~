pub fn handle_command(state: &mut crate::State) {
    // Match the command
    match state.line.trim() {
        "q" => state.done = true,
        "h" => io::print_error(&state.error),
        _ => {
            state.error = Some("Unknown command".to_string());
            println!("?")
        },
    }
}
